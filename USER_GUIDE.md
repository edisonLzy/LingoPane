# Translator 使用指南与说明文档 (User Guide)

一款专为 macOS 打造的高性能、轻量级个人 AI 翻译与长难句句法理解工具。
严格遵循 **Apple Human Interface Guidelines (HIG)** 与 **Liquid Glass（流体毛玻璃）** 物理设计语言，纯 Swift 原生打造，**零第三方库依赖**，并基于标准化 OpenAI 协议原生支持接入 **MiniMax 大模型**。

---

## 目录
1. [功能亮点](#1-功能亮点)
2. [快速启动与运行](#2-快速启动与运行)
3. [三大核心模式详解](#3-三大核心模式详解)
   - [模式一：英语单词深度解析](#模式一英语单词深度解析)
   - [模式二：英语长难句句法分析](#模式二英语长难句句法分析)
   - [模式三：中译英双场景直出](#模式三中译英双场景直出)
4. [MiniMax 大模型配置指南](#4-minimax-大模型配置指南)
5. [快捷预设与交互操作](#5-快捷预设与交互操作)
6. [开发者指南与编译构建](#6-开发者指南与编译构建)

---

## 1. 功能亮点

* **极简纯粹，无语言切换器**：内置智能语言与语法实体自动嗅探，无需任何手动选语种（中/英）操作。
* **物理级 Liquid Glass 美学**：
  * 透镜级真实毛玻璃模糊，窗口透明化，透视你的 macOS 桌面与背景窗口；
  * 镜面边缘高光（Specular Rim）与顶部物理环境光折射（Ambient Sheen）；
  * 严格遵循内嵌同心圆角几何比例（外框 $R=22\text{pt}$，内卡片 $R=16\text{pt}$）。
* **丝滑触觉弹簧动效**：发音声波实时跳动、鼠标悬停彩线升腾气泡、同义词展开丝滑旋转、流体微光扫光骨架屏（Shimmer）。
* **零依赖 & 极度轻量**：仅依赖 macOS 官方原生 Framework（SwiftUI、AppKit、AVFoundation），Release 可执行文件**仅 1.4 MB**。
* **双模式常驻**：支持独立浮动窗口与 macOS 顶部菜单栏状态图标（`MenuBarExtra`）。

---

## 2. 快速启动与核心划词操作 (⌥ Space)

### 2.1 跨应用划词唤起（核心工作流）
1. 在 macOS 上的**任意软件**中（例如 **VS Code、Safari、Chrome、Notion、飞书、终端** 等）选中一段英文或中文；
2. 按下全局快捷键 **`⌥ Space`（Option + 空格键）**；
3. Liquid Glass 悬浮翻译面板**将在鼠标光标附近瞬间弹现**：
   - 优先通过 macOS Accessibility API 零感知提取选中文本；
   - 遇到 VS Code / Chrome 等部分 Electron 应用时自动走 `⌘C` 模拟兜底（并自动恢复用户原剪贴板内容，完全无干扰）；
   - **首屏只呈现最核心结果**（原文、发音、翻译、一键复制）；
   - 无论何时，按 **`Esc`** 键或**鼠标点击浮窗外部**，浮窗立即收起，焦点无缝保留在当前宿主应用。

### 2.2 辅助功能权限配置（首次使用）
跨应用读取划选文本需要 macOS 辅助功能权限。
* 若首次按下 `⌥ Space` 未能读取文本，浮窗会自动展示权限指引；
* 点击 **“打开系统设置”**，在【隐私与安全性 → 辅助功能】中允许 `Translator` 即可生效。

### 2.3 状态栏图标与独立窗口
* **菜单栏图标**：macOS 顶部状态栏常驻小气泡 `💬`，点击亦可随时唤起；
* **未选中文本兜底**：在未划选任何文字时按下 `⌥ Space`，浮窗将显示原地输入框，供用户直接输入或粘贴翻译。

---

## 3. 三大核心模式详解

### 模式一：英语单词深度解析
当在输入框输入单个英文单词（如 `symmetric`, `volcano`, `deciduous` 等）并回车：

```text
┌─────────────────────────────────────────────────────────────┐
│ 单词解析                                                  ✕ │
├─────────────────────────────────────────────────────────────┤
│ symmetric                         /sɪˈmet.rɪk/          [🔊]│
│ 词根溯源：sym- (共同) + metr (测量) + -ic → 尺度对称的         │
│ [adj.] 对称的；匀称的；两边大小形状完全一致的                 │
│ [n.]   (数/物) 对称图形；对称结构                           │
├─────────────────────────────────────────────────────────────┤
│ ▾ 同义词与例句 (点击可折叠/展开)                              │
│   同义词辨析: [balanced] [proportional] [harmonious]        │
│   语境例句:                                                 │
│   • Mount Fuji presents a beautiful, nearly symmetric ...   │
│     从远处看，富士山呈现出优美、近乎对称的轮廓。             │
└─────────────────────────────────────────────────────────────┘
```

1. **单词与音标**：醒目展示单词原形，音标采用等宽 SF Mono 字体排版。
2. **朗读发音 `[🔊]`**：点击调用系统原生 TTS 发音。在发音期间，图标伴随声波弹性跳动。
3. **词根溯源**：独立微透底色卡片，拆解前缀、词根与后缀演化逻辑。
4. **多词性精析**：分行清晰罗列 `adj.`、`n.`、`v.` 等不同词性及对应释义。
5. **可折叠同义词与例句**：点击右侧箭头丝滑旋转展开，展示同义词胶囊标签（鼠标悬停放大微光）及中英对照语境例句。

---

### 模式二：英语长难句句法分析
当在输入框输入完整英文句子（如长难句、学术句子）时：

```text
┌─────────────────────────────────────────────────────────────┐
│ 句法分析                                                  ✕ │
├─────────────────────────────────────────────────────────────┤
│ [主语 (Subject)]                                            │
│ The volcanic cone   rises   gracefully   up to 12,000 feet. │
│ ─────────────────   ─────   ──────────   ────────────────── │
│      (蓝色)         (橙色)     (灰色)          (灰色)    [🔊]│
│                                                             │
│ ─ 主语   ─ 谓语   ─ 宾语/表语   ─ 状语   ─ 从句              │
├─────────────────────────────────────────────────────────────┤
│ 优美流畅的中文全句翻译...                                    │
├─────────────────────────────────────────────────────────────┤
│ ▾ 重点单词与短语 (点击可折叠/展开)                            │
│   核心词汇:                                                 │
│   • volcanic   adj.   火山的；火山作用引起的                 │
│   • gracefully adv.   优美地；端庄自然地                     │
│   关键短语搭配:                                             │
│   • up to             达到；攀升至（数量/高度）              │
│   • more than         超过；略多于...                        │
└─────────────────────────────────────────────────────────────┘
```

1. **彩线下划线成分分析**：
   * **主语 (Subject)**: 经典蓝色实线下划线
   * **谓语 (Predicate)**: 活力橙色实线下划线
   * **宾语/表语 (Object/Predicative)**: 清新绿色实线下划线
   * **状语 (Adverbial)**: 沉稳灰色细下划线
   * **从句 (Clause)**: 梦幻紫色实线下划线
2. **Hover 气泡浮窗**：鼠标掠过任一下划线成分，正上方立即浮现半透明 Liquid Glass 胶囊标签，显示具体语法角色；移出即刻平滑隐退。
3. **重点词汇与短语提取**：点击底部可折叠栏，展开提炼出的核心高频生词和重点短语搭配用法。

---

### 模式三：中译英双场景直出
当输入中文词语或句子时（如 `这个方案可以先作为一个兜底方案。`）：

```text
┌─────────────────────────────────────────────────────────────┐
│ 英文翻译                                                  ✕ │
├─────────────────────────────────────────────────────────────┤
│ [自然日常沟通]                                     [🔊] [📋]│
│ We can use this as a fallback for now.                      │
├─────────────────────────────────────────────────────────────┤
│ [正式书面设计]                                     [🔊] [📋]│
│ This can serve as a fallback solution in the interim.       │
└─────────────────────────────────────────────────────────────┘
```

1. **双场景直出**：
   * **自然日常沟通**：口语、日常讨论、Slack 聊天中地道、简练的表达方式。
   * **正式书面设计**：PRD 文档、架构说明、商务邮件中地道、严谨的高级表达。
2. **一键复制 `[📋]`**：点击后图标瞬间切换为绿色勾号 `✓` 并带弹性缩放，屏幕底部同步弹出 Liquid Glass 微气泡 Toast 提示。
3. **独立发音 `[🔊]`**：每句均可单独朗读纯正美式发音。

---

## 4. MiniMax 大模型配置指南

软件支持**全自动大模型智能解析**与**离线演示兜底**无缝切换。

### 方式一：界面图形化配置（推荐）
1. 点击主界面右上角的**齿轮图标（⚙️）**打开设置面板；
2. **MiniMax API Key**：填入你的 API Key（例如从 MiniMax 控制台获取的 Key）；
3. **API Host**：
   * 国内用户：`api.minimax.cn`（默认）
   * 海外用户：`api.minimax.chat`
   * 也可指向任何兼容 OpenAI 协议的中转/代理服务器
4. **模型**：默认为 `MiniMax-M3`（亦可点击下方药丸一键切换为 `MiniMax-Text-01` 或 `gpt-4o-mini`）；
5. 点击 **“测试 API 连接”** 按钮验证网络连通性；
6. 点击 **“完成”** 即可生效。

### 方式二：环境变量注入
在终端启动时直接注入环境变量：
```bash
export MINIMAX_API_KEY="your-api-key-here"
open Translator.app
```

> **提示（离线演示模式）**：
> 若尚未填入 API Key，应用绝不会报错崩溃，而是**自动启用内置的高保真离线数据库**，完美呈现包含 `symmetric`、`volcano`、火山锥从句、日本鬣羚长句、中文兜底方案等全部演示样例！

---

## 5. 快捷预设与交互操作

* **快捷预设药丸**：输入框正下方排布了常用的测试词句药丸，鼠标轻移其上带有流体微动弹簧，点击即可一秒体验不同面板效果。
* **顶栏 ✨ 预设菜单**：包含单词、长难句和中译英样例的完整下拉菜单。
* **清空输入**：点击输入框右侧的 `✕` 按钮瞬间复位。
* **快捷按键**：
  * `Enter / Return`: 提交分析；
  * `⌘ + C`: 复制选中文本；
  * `Esc`: 关闭弹窗或退出。

---

## 6. 开发者指南与编译构建

本项目采用纯原生 Swift 6 模块化结构，源码位于 `Sources/Translator/`：

### 执行单元测试
```bash
swift test
```
包含 6 项核心测试（OpenAI 协议生成、单词/句子/中文 JSON 反序列化、Mock 数据集对齐等），均在毫秒级执行通过。

### 重新编译 Release 可执行文件
```bash
swift build -c release
```

### 重新打包 macOS 独立应用
```bash
# 复制最新编译二进制并刷新签名
cp .build/release/Translator Translator.app/Contents/MacOS/
codesign -s - --force Translator.app
```
编译产物位于 `Translator.app`，体积仅 1.4 MB，启动秒开，内存占用极低。
